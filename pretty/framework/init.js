let inputBox = new Vue({
    el: "#inputBox",
});

let searchTree = new Vue({
    el: "#tree",
});

let inputSearch = new Vue({
    el: "#input",
});

let resultBox = new Vue({
    el: "#result",
    veutify: new Vuetify(),
});